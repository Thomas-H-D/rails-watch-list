<h1>All the Lists in One Place!</h1>

<ul>
<% @lists.each do |list| %>
<li><%= link_to list.name, list_path(list) %></li>
<% end %>
</ul>

<%= link_to "Make a new list", new_list_path, class: "btn btn-primary" %>
